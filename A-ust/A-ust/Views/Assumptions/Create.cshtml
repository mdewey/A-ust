@model A_ust.Models.Assumptions

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Assumptions</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Assumption)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Assumption)
            @Html.ValidationMessageFor(model => model.Assumption)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.TargetDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TargetDate)
            @Html.ValidationMessageFor(model => model.TargetDate)
        </div>
        @Html.HiddenFor(model => model.FeatureId, new {value = ViewBag.FeatureId})
        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
